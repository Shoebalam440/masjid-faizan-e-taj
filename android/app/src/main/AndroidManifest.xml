<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>

    </application>

    <!-- ===== CORE PERMISSIONS ===== -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- ===== NOTIFICATION PERMISSIONS ===== -->
    <!-- Android 13+ par runtime notification permission required hai -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Vibration for notifications -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- ===== EXACT ALARM / AZAAN PERMISSIONS ===== -->
    <!-- Android 12 (API 31) par exact alarms ke liye zaroori -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Android 14 (API 34) par alarm/clock apps ke liye - Azaan app qualify karta hai -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- ===== BACKGROUND EXECUTION ===== -->
    <!-- Phone restart ke baad notifications restore karne ke liye ZAROORI -->
    <!-- Capacitor's NotificationRestoreReceiver tabhi kaam karta hai -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Deep sleep mein bhi processor jagane ke liye -->
    <uses-permission android:name="android.permission.WAKE_LOCK" />

</manifest>
